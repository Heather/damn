#!/bin/bash

for file in "./damnation/${1}/*"
do
    cp "/usr/portage/${1}/${file}" "/usr/portage/${1}/${file}.damn"

	"./damnation/${1}/${file}"
	emerge -av ${1}

	cp "/usr/portage/${1}/${file}.damn" "/usr/portage/${1}/${file}"
done
